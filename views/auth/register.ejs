<%# Mẫu giao diện register kết hợp dữ liệu máy chủ với HTML để hiển thị một màn hình của ứng dụng. %>
<section class="auth-shell">
  <div class="auth-layout">
    <aside class="auth-copy">
      <p class="eyebrow">Tài khoản học sinh</p>
      <h2>Bắt đầu bằng đúng lớp đang học.</h2>
      <p>Hệ thống dùng khối lớp để mở chương trình phù hợp và theo dõi tiến trình luyện tập của từng học sinh.</p>
      <ul>
        <li>Chọn <%= gradeRangeLabel %> khi tạo tài khoản</li>
        <li>Học lý thuyết trước khi luyện câu hỏi</li>
        <li>Xem lại lỗi sai để ôn đúng trọng tâm</li>
      </ul>
    </aside>

    <div class="auth-panel">
      <p class="eyebrow">Đăng ký học tập</p>
      <h1>Tạo tài khoản học sinh</h1>
      <form class="stack-form" action="/auth/register" method="post" data-password-match-form>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label>
          Họ và tên học sinh
          <input name="fullname" autocomplete="name" inputmode="text" minlength="2" maxlength="100" data-fullname-policy required>
        </label>
        <label>
          Tên đăng nhập
          <input name="username" autocomplete="username" minlength="3" maxlength="50" required>
        </label>
        <div class="two-fields">
          <label>
            Mật khẩu
            <span class="password-field">
              <input name="password" type="password" autocomplete="new-password" minlength="10" maxlength="72" data-password-policy data-password-primary required>
              <button type="button" class="password-toggle" data-password-toggle aria-label="Hiện mật khẩu" aria-pressed="false">
                <i data-lucide="eye" class="lucide-icon"></i>
              </button>
            </span>
          </label>
          <label>
            Nhập lại mật khẩu
            <span class="password-field">
              <input name="confirmPassword" type="password" autocomplete="new-password" minlength="10" maxlength="72" data-password-confirm required>
              <button type="button" class="password-toggle" data-password-toggle aria-label="Hiện mật khẩu" aria-pressed="false">
                <i data-lucide="eye" class="lucide-icon"></i>
              </button>
            </span>
          </label>
        </div>
        <label>
          Khối học hiện tại
          <%# Bắt buộc tự chọn lớp thay vì mặc định sẵn Lớp 1: chọn nhầm lớp là
              thấy sai toàn bộ chương trình và phải nhờ người lớn sửa lại. %>
          <select name="grade" required>
            <option value="" selected disabled>— Em học lớp mấy? —</option>
            <%# Khối xử lý trong mẫu quyết định nội dung giao diện dựa trên dữ liệu máy chủ. %>
            <% gradeOptions.forEach((grade) => { %>
              <option value="<%= grade.value %>"><%= grade.label %></option>
            <% }) %>
          </select>
        </label>
        <p class="form-note">Mật khẩu tối thiểu 10 ký tự, có chữ hoa, chữ thường, chữ số và ký tự đặc biệt; không dùng khoảng trắng.</p>
        <button class="btn btn-primary full-width" type="submit">
          <i data-lucide="user-plus" class="lucide-icon"></i>
          Tạo tài khoản và vào bảng học tập
        </button>
      </form>
      <div class="auth-links">
        <a href="/auth/student/login">Đã có tài khoản?</a>
      </div>
    </div>
  </div>
</section>
<%
  if (typeof pageStyles !== 'undefined') pageStyles.push('/css/auth/common.css');
  if (typeof pageScripts !== 'undefined') pageScripts.push('/js/auth/common.js');
%>
